# Sample documents

This directory contains small public-domain texts used for testing.

Larger scripture collections should be obtained and stored separately by
the user. Run `scripts/download_bibles.py` for a guided downloader that
expects you to supply your own legally sourced files.
